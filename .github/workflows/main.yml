# This is a basic workflow to help you get started with Actions

name: ESLint
on:
  push:
  pull_request:
    branches:
      - main
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install ESLint
      run: yarn
    - name: Run ESLint
      run: eslint ./source/js/ --ext .js -c ./.eslintrc.js
